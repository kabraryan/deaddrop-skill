services:
  - type: web
    name: deaddrop
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DD_ADMIN_TOKEN
        sync: false
